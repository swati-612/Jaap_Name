<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.naamjaap">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.SandeshActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionbar"></activity>

        <activity android:name=".activities.AbTkJaapActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionbar"></activity>

        <activity android:name=".activities.AajKaJaapActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionbar"></activity>

        <activity android:name=".activities.JaapActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionbar"></activity>
        <activity
            android:name=".activities.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionbar" />
        <activity
            android:name=".activities.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionbar" />
    </application>

</manifest>